# Redrob AI — Intelligent Candidate Ranking Engine
# Python 3.10+  |  Design rule: stdlib-first

# --- Core ML ---
rapidfuzz>=3.6.1           # Fuzzy matching for skill/title taxonomy lookup
scikit-learn>=1.4.0        # TF-IDF for career description relevance scoring
numpy>=1.26.0              # Required by scikit-learn; used for score arrays
sentence-transformers>=2.6.0  # Semantic similarity (candidate ↔ JD embeddings)
torch>=2.2.0               # CPU-only; required by sentence-transformers

# --- Backend API ---
fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0.0

# --- Testing ---
pytest>=8.1.0
pytest-cov>=5.0.0
